/**
 * SOMA — Frontend Configuration
 * Centralizes public frontend settings.
 * Load this before any other script that depends on SOMA_CONFIG.
 */
const SOMA_CONFIG = {
    support: {
        href: "https://wa.me/{link}", // Reemplaza {link} con tu número o enlace de WhatsApp
        label: "¿Necesitas soporte?",
        target: "_blank",
    },
};